{
  "name": "@neoba/daemon",
  "version": "0.1.0",
  "description": "neoba daemon 参考实现:供给 + 执行 + 审批执行 + 观测(事实层)",
  "type": "module",
  "bin": { "neoba": "src/cli/main.ts" },
  "engines": { "node": ">=22.6" },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.17.1",
    "typescript": "^5.6.0"
  }
}
